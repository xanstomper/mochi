---
name: docker-deploy
description: Dockerization and deployment workflow
tools:
  - shell
  - write
  - edit
  - read
---

# Docker Deploy

Dockerization and deployment workflow. Covers: Dockerfile best practices (multi-stage, layer caching), docker-compose, environment management, health checks, nginx reverse proxy config, CI/CD pipeline setup (GitHub Actions), deployment to VPS/cloud.
