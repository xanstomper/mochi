---
name: api-design
description: RESTful + GraphQL API design patterns
tools:
  - write
  - edit
  - shell
  - web-search
---

# API Design

RESTful + GraphQL API design patterns. Covers: route structure, versioning, auth (JWT/OAuth), rate limiting, OpenAPI spec generation, input validation (zod/joi), error format standards, pagination patterns.
