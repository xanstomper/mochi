# Enforce package.json "engines" (node >= 22): installing on an unsupported
# Node major fails loudly here instead of surfacing cryptic runtime errors.
# See .nvmrc and .github/workflows/ci.yml for the pinned version.
engine-strict=true